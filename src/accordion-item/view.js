/*
document.addEventListener('DOMContentLoaded', () => {

	const accordions =
		document.querySelectorAll('.wp-accordion-item');

	accordions.forEach((accordion) => {

		const header =
			accordion.querySelector('.wp-accordion-header');

		const icon =
			accordion.querySelector('.wp-accordion-icon');

		header.addEventListener('click', () => {

			accordion.classList.toggle('active');

			const iconType =
				icon.dataset.icon;

			console.log(iconType);

		});

	});

});

*/